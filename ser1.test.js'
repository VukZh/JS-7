var series = (a, s1) => {
    if (a > 0) {
        return s1()
    } else {
        return sEnd()
    }
};
var s1 = () => ('next func');
var sEnd = () => ('last func');

describe('Check series func', () => {
    test('is next', () => {
        expect(series(1, s1))
            .toBe('next func');
    });

    test('is last', () => {
        expect(series(0, s1))
            .toBe('last func');
    });
});